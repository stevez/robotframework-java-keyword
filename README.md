# robotframework-java-keyword
sample code of creating robotframework keyword library in java, using maven plugin

To run the test, type following command:

mvn robotframework:run

mvn integeration-test
